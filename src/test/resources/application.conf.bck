testconf = {
  database = {
    url = "jdbc:postgresql://localhost:5432/postgres?user=tictactoe&password=tictactoe&currentSchema=tictactoe"
    driver = org.postgresql.Driver
    numThreads = 10
    connectionTimeout = 5000
    validationTimeout = 5000
  }

}
