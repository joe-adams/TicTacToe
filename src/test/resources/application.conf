conf = {
  databaseTest = {
    url = "jdbc:h2:./test"
    driver = org.h2.Driver
    numThreads = 20
    connectionTimeout = 10000
    validationTimeout = 10000
  }

  dispatcher {
    fork-join-executor {
      parallelism-factor = 2
      parallelism-max = 20
    }
  }

}
