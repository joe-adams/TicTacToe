testconf = {
  database = {
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    properties = {
      databaseName = "postgres"
      user = "tictactoe"
      password = "tictactoe"
    }
    numThreads = 10
  }
}

